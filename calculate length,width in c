#include<stdio.h>
int main()
{
	int length,width,area;
	printf("give the value of length");
	scanf("%d", & length);
	printf("give the value of width");
	scanf("%d", & width);
	area=length*width;
	printf("result =%d", area);
	return 0;
	
}